import { Grid } from "@mui/material";
import styled from "styled-components";

export const StyledTabLinks = styled(Grid)`
    display: flex;
    flex-direction: row;
    justify-content: space-between;
`;

export const StyledCardsLinks = styled(Grid)`
    display: flex;
    flex-direction: row;
    justify-content: space-evenly;
    height: 280px;
`;